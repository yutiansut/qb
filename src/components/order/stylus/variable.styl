@import "../../../common/css/variable.styl"

$order-bg-main = $normal-black-color  // 背景颜色
$order-bg-active = $main-bg-active // tab标签选中颜色、order列表表头颜色
$order-bg-detail = $bg-dark-color //  详情页背景

$order-line = $line-dark  // 分割线颜色

$order-font-title = $normal-white-color-a //表格表头、输入框颜色、输入框label颜色
$order-font-content = $normal-white-color-a9 // 标题颜色、表格内容颜色、订单详情颜色
$order-font-detail = $normal-gray-color // 订单详情内容颜色
$order-font-active = $main-color  // 主题色

$order-image-nothing = $icon-nothing
$order-image-close = $close-button